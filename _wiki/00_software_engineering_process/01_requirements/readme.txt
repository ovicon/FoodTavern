Non-functional:
- A network delay feature must be implemented to show the behaviour of the asynchronous implementation
- The mobile client is an Android application
- The desktop client is a JavaFX application
- The web application is an HTMl5, CSS3, JavaScript application
- etc

Functional:
- A user can use an android application to get a random food item, add a new one, or display all.
- A user can use an desktop application to get a random food item, add a new one, or display all.
- A user can use and web application to get a random food item, add a new one, or display all.
- etc

A business analyst must handle the requirements clarification with the client.
A scrum master must handle this requirement list further.